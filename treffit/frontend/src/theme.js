/**
 * Токены оформления.
 *
 * Серые — нейтральные, без синевы. Раньше вся палитра была одного синего
 * оттенка на низкой насыщенности: фон синеватый, линии синеватые, серый
 * текст синеватый и даже «чёрный» синий. Один тон во всём читается как
 * выцветшая картинка — контраста нет нигде, и взгляду не за что зацепиться.
 *
 * Теперь наоборот: нейтральный фон, почти чёрный текст и один насыщенный
 * акцент. Цвет должен обозначать действие, а не заливать собой интерфейс.
 */

export const T = {
  bg: "#F5F5F7",
  surface: "#FFFFFF",
  surfaceSoft: "#EFEFF2",
  // Почти чёрный, а не синий: заголовки должны быть тяжёлыми.
  ink: "#101014",
  muted: "#8A8A93",
  gold: "#0F9B58",
  goldSoft: "#DFF5E8",
  // Акцент один на всё приложение. Зелёный достаточно тёмный, чтобы белый
  // текст на нём читался: на светло-салатовом он бы поплыл.
  coral: "#12B76A",
  coralDeep: "#0E9455",
  lavender: "#34D399",
  line: "#E7E7EC",
  danger: "#F04438",
  success: "#12B76A",
};

export const gradient = {
  action: `linear-gradient(135deg, ${T.coral}, ${T.coralDeep})`,
  precious: `linear-gradient(135deg, ${T.coral}, ${T.gold})`,
  page: "linear-gradient(160deg, #FAFAFB 0%, #F2F2F5 100%)",
};

export const FALLBACK_GRADIENT = "linear-gradient(135deg,#C7F5DD,#12B76A)";
