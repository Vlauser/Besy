fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
aiosqlite==0.20.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
pyjwt==2.10.1
pillow==11.0.0
httpx==0.28.1
greenlet==3.1.1
redis==8.1.0

# Local NSFW screening. The ONNX weights ship inside the wheel, so there is
# no model download at runtime and no network dependency in production.
nudenet==3.4.2

# dev / tests
pytest==8.3.4
pytest-asyncio==0.25.0
